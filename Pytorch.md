# Pytorch

<font color='red'>***任何计算机系统，都无外乎由 “输入” “处理” “输出” 组成！***</font>

## 设计思想

Tensor -> Autogard -> nn.Module

高维数组 -》自动求导 -》神经网络

## 基础概念

### 模型训练

*模型训练本质上在干嘛？*

1. **模型给出预测（比如概率 0.8）**
2. **计算“错得有多离谱”（loss）**
3. **调整参数，让下次错得更少**

### Tensor

- ***标量、向量、矩阵都是张量，只是维度不同，Tensor包含多维数组，模型输入/输出都是张量***
- **维度变换**：
  - view：只能用于内存中连续存储的Tensor，对于不连续的Tensor应先将其连续化tensor.contiguous.view()
  - reshape：不要求Tensor在内存中是连续的，直接使用即可。
- **转置：**行变列，列变行
  - self.W =
    [
      [1, 2, 3],
      [4, 5, 6]
    ]
  - self.W.T =
    [
      [1, 4],
      [2, 5],
      [3, 6]
    ]
- **类型：**
  - 设备类型（device）-- CUDA/CPU
  - 数据类型（dtype）-- bool/float/int
- **命名张量：**推荐使用维度的名称进行维度操作，这样可以避免重复计算Tensor每个维度的位置
- **Numpy：**可以与numpy数组转换，当某些场景下Tensor不支持的操作可以先转换为numpy进行处理，然后转回Tensor

### Shape

在每一个维度上的长度 

```python
标量：x = 5 → shape = ()

向量：x = [10, 20, 30] → shape = (3)

矩阵：

[
[1, 2, 3],
[4, 5, 6]
] → 
shape = (2, 3)

三维张量：

[
[[a, b],
[c, d]],

[[e, f],
[g, h]],

[[i, j],
[k, l]]
] → 
shape = (3, 2, 2)
```

### 反向传播

出了结果以后，倒着查“谁该为错误负责” 

在计算图上应用链式法则（*原理过于深奥，不做深入了解*）

用一次 backward，自动算出所有参数，“该往哪调、调多少”。

### 梯度

**torch.grad**

- 一句话抓住本质：对每一个输入维度分别求导，然后把结果“打包”成一个向量，往哪个方向改一点，结果变化最快。
- 求所有变量的偏导数，并将这些偏导数汇总为向量，就是梯度
- 梯度“数值大小”意味着什么？
  - 梯度大 → 稍微动一点，结果变化很大
  - 梯度小 → 动半天，结果几乎不变

**梯度爆炸问题**

- grad在反向传播过程中是累加的，因此每次进行反向传播之前需要把梯度清零

**梯度下降**

- 贪心算法：无法找到非凸函数的最优点（只能找到局部最优点无法找到全局最优）

**梯度消失**

- 鞍点：梯度为0 无法更新，但是此时可能只是处于水平线没有到达最低点。

### AutoGrad

- **导数：**y = f ( x )，在某一点的斜率（变化率）
- **微分：**输入值的变化引起输出值的变化量
- **偏导数：**当函数存在多维输入时，一个输入的变化（保持其余输入不变），输出值的变化率

- **通俗理解**：拧水龙头(参数X) -》龙头出水(结果Y)，现在 关心的是 我把水龙头拧紧一点/松一点，水量会变化多少？auto_grad就是自动化的告诉你拧一点点，水量的变化情况

### Loss

- Mean Square Error: 均方误差

  MSE = (1/n) * Σ(预测值 - 真实值)² 

- 随着轮次增加/Loss没有收敛反而发散了（考虑降低学习率）

### 全连接层

每个输出神经元，都连接到每个输入神经元

```pyth
y1 = w11*x1 + w12*x2 + w13*x3
y2 = w21*x1 + w22*x2 + w23*x3
# x1 ->
# x2 -> 全连接 ↗ y1
# x3 ->       ↘ y2
```

### 神经网络

#### 类型

##### MLP（多层感知机, Multi-Layer Perceptron）

**核心概念**：

- 最基础的神经网络，由 **全连接层（Fully Connected, FC）** 叠加而成
- 每个神经元和上一层的每个神经元都有连接

**特点**：

- 输入数据需要**展平成一维向量**
- 对空间/序列结构没有天然感知
- 通用，但不擅长处理图片、语音、文本这种有“局部结构”的数据

**擅长任务**：

- 表格数据预测（房价预测、信用评分）
- 一般分类/回归问题

**工程实践:**

- 一般最多5层：Linear → ReLU → Linear → ReLU → Linear
- nn.Linear(input, 128)通过经验和约束来设定128这个参数值(32/64/128)

| input   | 合理 hidden |
| ------- | ----------- |
| 5 ~ 10  | 32 / 64     |
| 20 ~ 50 | 64 / 128    |
| 100+    | 128 / 256   |

| 样本量     | hidden 上限建议 |
| ---------- | --------------- |
| < 1k       | 16 ~ 64         |
| 1k ~ 10k   | 64 ~ 128        |
| 10k ~ 100k | 128 ~ 256       |
| > 100k     | 256+            |

##### CNN（卷积神经网络, Convolutional Neural Network）

**核心概念**：

- 核心是 **卷积层（Convolutional Layer）**
- 通过 **卷积核** 在输入数据上滑动，提取 **局部特征**
- 常用于 **二维（图片）或一维（语音/时间序列）** 数据

**特点**：

- 利用 **局部相关性**（邻近像素/时间点更相关）
- 参数共享 → 大幅减少模型参数
- 对空间结构非常敏感（平移不变性）

**擅长任务**：

- 图像分类、目标检测
- 视频、语音信号处理
- 图像生成（GAN、超分辨率）

##### LSTM（长短时记忆网络, Long Short-Term Memory）

**核心概念**：

- 一种 **循环神经网络（RNN）**，专门处理 **序列数据**
- 内部有 **记忆单元（Cell State）** 和 **门控机制（输入门、遗忘门、输出门）**
- 能捕捉 **长时间依赖**，解决普通 RNN 梯度消失问题

**特点**：

- 输入数据可以是时间序列或文本序列
- 擅长学习序列中的顺序关系
- 参数量相对较大，训练需要更多计算

**擅长任务**：

- 语音识别、文本生成
- 股票预测、时间序列预测
- 机器翻译、对话生成

#### 拟合函数

找一个公式，让输入 x 和输出 y 的关系尽可能匹配： y = f ( x )

#### 卷积层

将特征值进行加权求和得到初步的预测结果，只能学习到简单的乘、加关系（线性 Linear/卷积）

**y = Wx + b**（W和b都是可学习参数，***矩阵的乘法只看矩阵的形状是否满足要求，所以需要转置y = x @ self.W.T # A @ B 表示矩阵乘法 self.W.T表示把W矩阵转置***）

每个输出都是所有输入的加权和

| 符号 | 含义                                                         |
| ---- | ------------------------------------------------------------ |
| x    | 输入数据（batch_size × in_features）                         |
| W    | 模型参数（权重矩阵，out_features × in_features，随机初始化，可训练） |
| W.T  | W 的转置，用来对齐矩阵乘法（x 的列数必须等于 W.T 的行数）    |
| b    | 偏置（每个输出一个偏置，自动广播到 batch 维度）              |
| y    | 输出数据（batch_size × out_features）                        |

对应到pytorch的Linear

`Linear`：一次算 **一整行数据**（更关注整体）

```python
nn.Linear(in_features=5, out_features=16)
# 输入 x (5维) → Linear(5,16) → 输出 y (16维)
# 输入 5 个特征 → 每个输出神经元做：
# y1 = w11x1 + w12x2 + ... + w15x5 + b1
# y2 = w21x1 + w22x2 + ... + w25x5 + b2
# ...
# y16 = w161x1 + ... + w165x5 + b16

# 输出 = 16 个神经元的结果 → 下一层或输出层
```

`Conv`：用 **同一套 Linear**，在数据上 **反复算很多次**（更关心局部，例如服务器备份数据占用量在某段时间内异常增长）

#### 激活层

曲线关系，让网络可以学习更加复杂的规律。

**ReLU()**

最受欢迎的激活函数是***修正线性单元*（Rectified linear unit，*ReLU*）**

ReLU ( x ) = max ( 0, x )

输入值x为负数是为0，输入值x为正数时为x

**本质上是让模型拥有：过了某个点，行为突然改变 的判断能力**

#### 池化层

下采样提取主要特征：

```python
# 如果你有很多房屋特征组合（比如同一片区、不同楼层、不同户型），池化帮你 挑出最能代表房价的特征，忽略微小波动。

# 直观理解：

# 同一片区10套房子，有房价差异：

# [500k, 520k, 510k, 495k]

# 池化（取最大） → 520k，抓到最显著的价格特征

# 池化（取平均） → 506k，平滑价格

# 在深度网络里，卷积提取特征 → 激活增加弯曲能力 → 池化保留关键特征，重复多层，就能学到复杂规律。
```

#### torch.nn

- **通用训练步骤：**

  - 定义一个包含可学习参数的神经网络：

    class继承nn.Module并实现forward函数。

    ```pyth
    class Net(nn.Module):
    # 对于含有可学习参数的层应该把它放在__init__()函数中
        def __init__(self):
            super().__init__()
            self.w = nn.Parameter(torch.tensor(1.0))
    # 不含有可学习参数的层既可以放在__init__()函数中也可以放在forward函数中
        def forward(self, x):
            return self.w * x
    ```

  - 加载用于训练该网络的数据集

  - 进行前向传播，得到网络输出结果，计算损失（输出结果与正确结果的差距）

    **损失函数：**

    torch.nn实现了绝大部分损失函数

    ```pyth
    import torch
    import torch.nn as nn
    
    # 简单损失函数 → 反向传播 → 溯源 → 拿到参数梯度
    # 这就是自定义的Net模型（继承了nn.Module能被pytorch识别）
    class Net(nn.Module):
        def __init__(self):
            super().__init__()
            self.w = nn.Parameter(torch.tensor(1.0))  # 一个可训练参数
    
        def forward(self, x):
            return self.w * x
    
    # 只有直接运行这个文件时，才执行下面的代码
    if __name__ == "__main__":
        x = torch.tensor(2.0)
        y_true = torch.tensor(8.0)
        model = Net()
        # 调用forward函数前向传播
        y_pred = model(x)
        # 损失函数
        loss = nn.MSELoss()(y_pred, y_true)
        # 反向传播
        loss.backward()
    
        print("loss = ", loss.item())
        print("w grad = ", model.w.grad)
        print("w grad item = ", model.w.grad.item())
    ```

    **Tips:** **梯度为正数 参数往小调 梯度为负数 参数往大调，重点关注参数调整后loss的变化**

  - 进行反向传播，更新网络参数

    **优化器：**

  - 训练完毕 保存网络模型

#### nn.Sequential

当你的网络可以看作一个“严格流水线”，一层接一层，就用它；否则写自定义 forward。

### 训练集/验证集/测试集

| 数据集 | 是否参与训练 | 是否反向传播 | 主要用途     |
| ------ | ------------ | ------------ | ------------ |
| 训练集 | ✅ 是         | ✅ 是         | 学参数       |
| 验证集 | ❌ 否         | ❌ 否         | 调参、选模型 |
| 测试集 | ❌ 否         | ❌ 否         | 最终评估     |

### Dataset

核心职责（只有 3 件事）

1. **定义样本总数**
2. **定义如何按 index 取一条样本**
3. **屏蔽数据来源差异（CSV / DB / 文件 / 内存）**

### DataLoader

核心职责

1. **batch 化**
2. **shuffle**
3. **多进程 / 多线程读取**
4. **prefetch**
5. **pin memory（CPU→GPU 加速）**

### Optimizer

- Adam

  

- SGD




### Matplotlib

- Loss曲线
- 权重分布
- 梯度分布

## 工程应用

### 基本流程

训练（state_dict）
↓
评估
↓
导出 TorchScript（推理专用）
↓
发布到 Java / DJL

### 转换模型选择

| 特性           | `torch.jit.trace`                                    | `torch.jit.script`                               |
| -------------- | ---------------------------------------------------- | ------------------------------------------------ |
| **适用场景**   | 前向推理固定图，模型中没有条件分支、循环等动态控制流 | 模型中有条件语句、循环、动态控制流               |
| **复杂度**     | 简单、快速                                           | 相对复杂，可能需要修改代码以兼容 TorchScript     |
| **性能**       | 高                                                   | 高                                               |
| **工程稳定性** | 对输入形状固定的业务场景非常稳定                     | 对复杂模型稳定性好，能保证行为和 Python 模型一致 |
| **推荐**       | 大部分工业中简单预测模型（例如房价、推荐、图像分类） | 复杂模型，或者模型里有 if/for/while 等动态逻辑   |

### 工程问题分类

| 问题类型                   | 你在干什么     | 输出是什么                 |
| -------------------------- | -------------- | -------------------------- |
| **回归（Regression）**     | 预测一个数     | 连续值（天数、价格、容量） |
| **分类（Classification）** | 判断属于哪一类 | 类别（A/B/C）              |
| **时间序列预测**           | 预测未来走势   | 未来数值                   |
| **决策 / 策略**            | 选一个最优动作 | 行为 / 策略                |

不同环境增长差异大 → 不必单独训练，**加环境特征 + 统一归一化 + 微调** 就能覆盖大部分情况

### 模型分层

#### 堆叠集成

- **做法**：第一层训练多个不同类型或不同数据子集的“基模型”，然后将这些基模型的预测输出作为新的特征（称为“元特征”），输入到第二层的“元模型”中进行最终预测。
- **关键**：基模型要尽可能“好而不同”，且元模型通常使用相对简单的线性模型或浅层树模型。

#### 流水线式处理

- **做法**：前一个模型的输出是后一个模型的输入，形成处理链。

#### 分层特征学习

- **做法**：深层神经网络的每一层都可以看作一个“简单模型”。浅层学习低级特征（如边缘、纹理），中层组合这些低级特征形成中级特征（如部件），高层再组合中级特征形成高级的、与任务高度相关的抽象表示，最后由最后的分类/回归层做出决策。

### 过拟合

- **概念**

​		当我们训练模型时，我们只能访问数据中的小部分样本，当收集到更多的数据时，会发现之前找到的明显关系并不成立。

​		用于对抗过拟合的技术叫做 *正则化（regularization）*

- **暂退法（Dropout）**

  



### K折交叉验证

当训练数据稀缺时，我们甚至可能无法提供足够的数据来构成一个合适的验证集。 这个问题的一个流行的解决方案是采用*K折交叉验证*。 这里，原始训练数据被分成K个不重叠的子集。 然后执行K次模型训练和验证，每次在K-1个子集上进行训练， 并在剩余的一个子集（在该轮中没有用于训练的子集）上进行验证。 最后，通过对K次实验的结果取平均来估计训练和验证误差。

### 鲁棒性

### 归一化

### 加噪声

### 监督学习/半监督/自主/强化

### 局部/全局最小值

### 鞍点